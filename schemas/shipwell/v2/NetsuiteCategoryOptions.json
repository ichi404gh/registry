{
  "description": "All available mapping options from Netsuite. Categories come from the 'Items' section in Netsuite",
  "properties": {
    "billing": {
      "description": "Valid categories for billing",
      "items": {
        "description": "Item category in Netsuite",
        "properties": {
          "display_name": {
            "description": "Human-readable name of the category",
            "type": "string"
          },
          "id": {
            "description": "Internal Netsuite ID",
            "type": "string"
          }
        },
        "type": "object"
      },
      "type": "array"
    },
    "invoicing": {
      "description": "Valid categories for invoicing",
      "items": {
        "description": "Item category in Netsuite",
        "properties": {
          "display_name": {
            "description": "Human-readable name of the category",
            "type": "string"
          },
          "id": {
            "description": "Internal Netsuite ID",
            "type": "string"
          }
        },
        "type": "object"
      },
      "type": "array"
    }
  },
  "type": "object",
  "default": {
    "billing": [
      {
        "display_name": "string",
        "id": "string"
      }
    ],
    "invoicing": [
      {
        "display_name": "string",
        "id": "string"
      }
    ]
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
