{
  "description": "Either a driver's auth token or a driver's phone number and pin",
  "properties": {
    "phone_number": {
      "description": "Optional phone number of the driver to link",
      "type": "string"
    },
    "pin": {
      "description": "Optional pin number of the driver to link",
      "format": "int32",
      "type": "integer"
    },
    "token": {
      "description": "Optional authentication token of the driver to link",
      "type": "string"
    }
  },
  "type": "object",
  "default": {
    "phone_number": "string",
    "pin": 1,
    "token": "string"
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
